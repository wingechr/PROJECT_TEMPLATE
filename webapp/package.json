{
  "name": "TODO",
  "version": "0.0.0",
  "license": "ICS",
  "type": "module",
  "browserslist": "> 0.5%, last 5 versions, not dead",
  "source": "src/index.html",
  "scripts": {
    "clean": "scons --clean && rm -rf dist build .parcel-cache",
    "test": "scons -j4 && npx mocha src/**/test*.js",
    "serve": "scons -j4 && npx parcel serve --dist-dir build",
    "build": "scons -j4 && rm -rf dist && npx parcel build --no-cache --no-scope-hoist --no-source-maps --dist-dir dist --public-url ./"
  },
  "dependencies": {
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "acorn": "^8.11.3",
    "acorn-walk": "^8.3.2",
    "chai": "^5.1.0",
    "chai-almost": "^1.0.1",
    "eslint": "^8.36.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-config-standard": "^17.1.0",
    "eslint-plugin-jsdoc": "^48.2.1",
    "eslint-plugin-prettier": "^5.1.3",
    "glob": "^10.3.10",
    "mocha": "^10.3.0",
    "parcel": "^2.12.0",
    "prettier": "^3.2.5"
  }
}
