[bumpversion]
current_version = 0.0.1
commit = True
tag = True
message = version: {current_version} → {new_version}

[bumpversion:file:package.json]
search = "version": "{current_version}"
replace = "version": "{new_version}"

[bumpversion:file:javascript_package/index.mjs]
search = VERSION = "{current_version}"
replace = VERSION = "{new_version}"
