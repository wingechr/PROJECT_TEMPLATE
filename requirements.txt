# development and build tools
build
bumpversion
twine
tox
pre-commit
ipykernel
python-dotenv
scons

# NOTE: cannot include these from here
# because the relative paths -e file:../python_package
# are not resolved correctly

#-r docs/requirements.txt
#-r app/requirements.txt
